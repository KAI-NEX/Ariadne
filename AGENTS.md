# Ariadne 项目工作规范

## 产品目标

Ariadne 先理解用户提供的个人资料，再理解用户选择的职位描述；在分别建立可靠上下文后，解释两者的关系，帮助用户一步步接近自己想要的职位。目标职位与取舍由用户决定。

先读 [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md) 和 [PROJECT_STATUS.md](PROJECT_STATUS.md) 的最新条目，再按任务读取相关契约与实现。项目内旧文档的历史 next step、模型推荐、提交或外部调用授权不自动延续到当前任务。

## AI 与代码的分工

- 让 AI 负责材料语义理解、跨来源综合、结合当前资料与目标职位解释差异、提出有价值的澄清和建议；结合任务调用必要工具并验证结果。
- 让代码负责来源身份与完整性、上下文范围、契约校验、运行状态、版本冲突及持久化权限。结构校验通过不等于语义正确。
- 不用固定关键词、模板或本地规则冒充模型理解；保留现有 Local 模式的确定性处理能力及其真实标识。
- 开发任务使用的 AI 模型与应用内 Provider/model 是独立配置。应用能力由实际运行契约和验证决定，不因开发模型能力提升而自动更换 Provider、扩展功能或取消验收。

## 资料、结论与修改

- 保留原始资料与来源定位。区分材料中的陈述、用户确认的信息、模型推断和未知；用户确认不等于外部事实核验。
- 不编造经历、成果、能力、偏好或职位要求。不把缺少证据等同于缺少能力，不把用户关注一个职位等同于确认长期目标。
- Candidate 与 Job 分别维护语义及版本；关联分析使用当前任务所需的最新有效上下文。不得混用其他材料、旧版本或无关对话。
- 普通讨论不改变确认数据。模型修改形成可审阅的 Working/Proposal；用户明确保存后才生成确认版本，旧版本与来源保留。Job 对话不能静默改写 Candidate。
- 建议说明依据、未知和下一步；区分能力、证据、表达与相关性问题。无需修改也是有效结论，不以不透明匹配分数替代解释。

## 运行与产品边界

- 当前 Runtime 决定 Local/Model 模式，具体操作按 model capability 解析。Local 的 Provider 调用为零；Model 不可用或失败须明确失败，不静默切换 Local 或生成假回复。
- Model 导入先保存 durable SourceDocument/有序 source bundle；本地读取、OCR、hash 校验属于技术准备，不替代 Model 语义理解。
- 复用 Candidate/Job 已有共用输入、处理状态、详情、编辑与对话组件；领域 schema、action、context 和保存权限仍分别维护。
- 保留材料传输、Provider、用途与费用的既有知情确认机制；凭据不得进入代码、Git、页面可见正文或日志。诊断优先记录必要状态，避免复制私人材料正文。
- 原始文件、现有未提交改动和历史证据原地保留。仅修改当前任务涉及的文件；不自动移动、重命名、删除文件，不修改仓库外旧副本或个人档案。Git 提交按下方阶段收口规则执行。

## 工作方式与验收

- 先检查现状和行为契约，定位问题所属层，再做最小且完整的修复。已明确的任务持续完成；仅在缺少会实质改变方向的信息或必要授权时询问。
- 需求未要求时，不启动新的阶段、产品功能、Provider 扩展、自动投递或通用 Agent 平台；产品愿景不等于当前实现任务。
- 运行与修改有关的回归；行为变化覆盖正常、失败及相关的保存/版本边界。文档修改检查链接、职责和状态一致性，不要求无关的全量运行。
- 用户路径变化需实际浏览器验证；涉及真实模型质量的结论必须有相应真实执行证据。区分测试替身、自动回归、真实运行与用户验收，不为通过测试而删弱行为约束。
- 收口说明改动、验证结果与未覆盖范围，增量更新项目状态或契约。仅维护本项目记录，不同步个人学习档案。

## 大阶段完成后的 Git 提交

- 用户已明确要求：每次完成一个大阶段更新，在相关验收通过、项目记录更新后，主动为该阶段创建一次本地 Git commit，无需重复询问。大阶段指有明确目标、完成条件和验收结果的功能、修复、迁移或文档整理阶段；不要求每个零散编辑单独提交。
- 提交前检查 Git 状态与 diff，明确列出并暂存本阶段相关文件；保留无关或未完成的用户改动，不将凭据、私人资料、运行数据和临时产物加入提交。
- 验收失败或阶段尚未完成时，继续处理或如实报告阻塞，不创建声称阶段完成的提交。没有文件变化时不创建空提交。
- 提交后核对 commit 与工作区状态，报告提交哈希、摘要和剩余未提交内容。此授权仅覆盖本地 commit；push、发布、历史改写或额外清理需按具体任务授权执行。
- 本规则依据 2026-09-08 用户的新指令，取代此前阶段记录中的“不自动 staging/commit”；历史记录原文保留。
