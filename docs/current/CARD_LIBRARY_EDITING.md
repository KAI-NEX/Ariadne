# 资料库卡片编辑与删除

2026-09-20。个人资料页与职位描述页在「了解我」「了解职位概况」右侧提供「编辑 / 完成」。编辑状态显示每张已存卡片的「删除」；添加材料入口保留，卡片详情和投递状态控件暂不响应，退出后恢复。

删除前展示卡片标题和确认范围。取消或 Escape 不写入；确认期间禁止重复提交。只有持久化成功才移除卡片；存储失败保留卡片并允许重试。若保存成功但列表更新失败，明确提示刷新，不冒充删除失败。

- Candidate 复用 `candidate_context_lifecycle`，按 context + item 移除；同一来源其他条目保留。
- 已确认 Job 新增 `job_context_lifecycle`，按 context 移除。记录包含用户权限、当前 revision、时间与原因，版本冲突及重复删除拒绝；不覆盖旧确认版本、不删除来源、对话或投递历史。
- 当前资料库、工作空间计数和职位概况排除已移除 Job。职位概况的当前指纹随范围改变，旧概况缓存不能冒充当前结果；已确认后移除的 proposal 不退回 Working。
- 旧演示/Local 卡片继续采用既有领域删除行为，不以演示数据写入确认资料。
- 主数据库契约版本 18；网页 Markdown 库按缺失 store 增量升级，保留原迁移标记、内容和原备份。本机文件库沿现有 schema 增加空记录集合。

仅实现当前卡片移除，不增加整份来源删除、恢复入口或批量选择。没有调用模型，不改变 Provider、材料传输确认或日常人工保存规则。浏览器验收使用隔离工作区与合成资料，不修改实际个人材料。

自动验证覆盖持久化契约、领域删除、Job 当前范围及缓存、版本冲突、重复移除、失败回滚；`tests/helpers/browser-storage-upgrade.mjs` 在真实浏览器验证旧 Markdown 库升级与重开。UI 证据在忽略目录 `.cache/library-edit-20260920/`，覆盖 1440×900 与 390×844、取消/确认/刷新、连续删除至空态、减少动效、键盘焦点与注入存储失败重试。
